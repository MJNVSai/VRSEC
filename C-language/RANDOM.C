#include<stdio.h>
#include<conio.h>
#include<stdlib.h>
#include<time.h>
int main()
{
	clrscr();
	srand(time(NULL));
	printf("random number is %d\n",rand());
	printf("random number is %d\n",rand());
	printf("random number is %d\n",rand());
getch();
}